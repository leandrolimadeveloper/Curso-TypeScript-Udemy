"use strict";
let aluno;
aluno = [1, 'Leandro', 9];
aluno.push('Cássio', 10, 2); // É possível alterar a ordem ao adicionar itens no array
console.log('Id', aluno[0], 'Nome', aluno[1], 'Nota', aluno[2]);
console.log(aluno);
let statusPedido = ['Em espera', 'Em progresso', 'Saiu para a entrega', 'Pedido entregue'];
console.log(statusPedido);
